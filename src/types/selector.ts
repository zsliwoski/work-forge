export interface Team {
    id: string;
    name: string;
    icon: string | null | undefined;
}
export interface Organization {
    id: string;
    name: string;
    icon: string | null | undefined;
}